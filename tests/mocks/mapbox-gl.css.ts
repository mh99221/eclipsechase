// Empty stub for `mapbox-gl/dist/mapbox-gl.css`. Map components import
// the stylesheet alongside the JS so production bundles ship it with
// the lazy chunk. The test runner has no DOM CSS to apply, so this
// returns an empty module — the alias in vitest.config.ts points here.
export {}
