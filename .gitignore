# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Script caches (raw API dumps, regenerable)
scripts/output/discovery-cache.json

# Raw Supabase row exports — regenerable from production, will go stale.
# Used as a local snapshot for audit/verify scripts in scripts/internal/.
scripts/*_rows.sql

# Intermediate JSON produced by scripts/internal/extract-spots-for-skyfield.mjs
# from the latest viewing_spots_rows.sql export. Regenerable.
scripts/internal/spots-for-skyfield.json

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
!.env.test

# Superpowers
.superpowers/

# RSA keys (local dev only — production keys go in Vercel env vars)
*.pem
de421.bsp

# DEM binary (856MB, too large for git)
server/data/dem/*.bin

# Raw photos — sources for scripts/process-spot-photos.mjs.
# Only the processed webps in public/images/spots/ are needed at runtime.
raw-photos/

.vercel
.env*.local

.claude/settings.local.json
playwright-report/
test-results/
supabase/.temp/
.nuxtrc
